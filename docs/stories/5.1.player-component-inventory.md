# Story 5.1: Player Component Inventory

## Status: Draft

## Story

**As a** player,
**I want** to add components to my personal inventory,
**so that** I can track all my dart equipment.

## Acceptance Criteria

1. PlayerComponent model linking Player to Component (or custom)
2. "My Components" section in profile
3. Add component from catalog (searchable dropdown)
4. Add custom component (manual entry with optional photo)
5. View all owned components grouped by type
6. Remove component from inventory

## Tasks / Subtasks

- [ ] Task 1: Create PlayerComponent Model & Migration (AC: 1)
  - [ ] Create PlayerComponent model
  - [ ] Create migration with player_id, component_id, is_custom, custom fields
  - [ ] Add relationships to Player and Component

- [ ] Task 2: Create EquipmentController (AC: 2, 3, 6)
  - [ ] Create `app/Http/Controllers/EquipmentController.php`
  - [ ] Implement index (list inventory)
  - [ ] Implement store (add from catalog)
  - [ ] Implement destroy (remove from inventory)

- [ ] Task 3: Create Inventory Views (AC: 2, 5)
  - [ ] Create `resources/views/pages/equipment/index.blade.php`
  - [ ] Display components grouped by type
  - [ ] Add "Add Component" button

- [ ] Task 4: Catalog Search (AC: 3)
  - [ ] Create searchable component dropdown
  - [ ] Filter by brand and type
  - [ ] Select and add to inventory

- [ ] Task 5: Add Routes
  - [ ] Add equipment resource routes

## Dev Notes

### Relevant Architecture References

**PlayerComponent Model:**
```php
- id: bigint unsigned
- player_id: bigint unsigned
- component_id: bigint unsigned nullable
- is_custom: boolean default false
- custom_brand: string nullable
- custom_name: string nullable
- custom_type: enum nullable
- custom_attributes: json nullable
- custom_image_path: string nullable
```

**Routes:**
```php
Route::resource('equipment', EquipmentController::class);
```

### Testing

**Test Location:** `tests/Feature/Equipment/InventoryTest.php`

## Change Log

| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 2026-01-08 | 1.0 | Initial story creation from PRD | Sarah (PO) |
